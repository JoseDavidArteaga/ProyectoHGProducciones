# HGProducciones - Sistema de Venta de Tickets con Seguridad
# Puerto del servidor Spring Boot
server.port=8080

# MySQL Database Configuration con XAMPP
spring.datasource.url=jdbc:mysql://localhost:3306/hgproducciones_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Configuración JPA
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# JWT Configuration (Base64 encoded secret for new JWT version)
jwt.secret=SEdQcm9kdWNjaW9uZXNTZWNyZXRLZXkyMDI0TXV5TGFyZ2FZUGF0YUZyaXRhQmFzZTY0
jwt.expiration=86400

# Legacy JWT Configuration (for old files)
hgproducciones.app.jwtSecret= hgproduccionesSecretKey
hgproducciones.app.jwtExpirationMs= 86400000

# CORS se configura en los controladores REST con @CrossOrigin
# Permitir conexiones desde Angular (http://localhost:4200)
